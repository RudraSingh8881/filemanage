MONGODB_URI="mongodb+srv://<user>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority"
JWT_SECRET="change_this_to_a_secure_random_string"
FRONTEND_URL="https://your-frontend.onrender.com"
PORT=5000

# Notes:
# - Copy this file to `.env` in `backend/` when running locally.
# - Never commit your real `.env` with secrets to source control.